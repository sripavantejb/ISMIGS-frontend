/**
 * Initial landing page (from landing page/analysis-frontend).
 * Renders the full landing; "Get started" / "View Dashboards" / "Explore" link to /dashboard.
 */
// @ts-ignore - landing is JSX
import HomePage from "@landing/pages/HomePage.jsx";

export default function LandingPage() {
  return <HomePage />;
}
